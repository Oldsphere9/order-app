# 커피빈 메뉴 주문 앱

커피빈 현대자동차남양연구소점을 위한 메뉴 주문 관리 시스템입니다.

## 🚀 빠른 시작

### 로컬 개발 환경 설정

1. **저장소 클론**
   ```bash
   git clone <repository-url>
   cd order_app
   ```

2. **데이터베이스 설정**
   ```bash
   cd server
   npm install
   # PostgreSQL 실행 후
   npm run setup-db
   npm run insert-menus
   npm run create-member-menu-preferences-table
   ```

3. **백엔드 서버 실행**
   ```bash
   cd server
   npm run dev
   # http://localhost:3000
   ```

4. **프론트엔드 실행**
   ```bash
   cd ui
   npm install
   npm run dev
   # http://localhost:5173
   ```

## 📦 프로젝트 구조

```
order_app/
├── server/          # 백엔드 (Express + PostgreSQL)
│   ├── src/
│   │   ├── config/      # 데이터베이스 설정
│   │   ├── controllers/ # 컨트롤러
│   │   ├── models/      # 데이터 모델
│   │   ├── routes/      # API 라우트
│   │   └── server.js    # 서버 진입점
│   └── package.json
├── ui/              # 프론트엔드 (React + Vite)
│   ├── src/
│   │   ├── components/  # React 컴포넌트
│   │   ├── pages/       # 페이지 컴포넌트
│   │   ├── hooks/       # 커스텀 훅
│   │   └── utils/       # 유틸리티 함수
│   └── package.json
└── docs/            # 문서
    ├── PRD.md           # 제품 요구사항 문서
    ├── CODE_REVIEW.md   # 코드 리뷰 보고서
    └── DEPLOYMENT.md    # 배포 가이드
```

## 🌐 배포

Render.com 배포 가이드는 [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md)를 참고하세요.

### 배포 순서
1. PostgreSQL 데이터베이스 생성
2. 백엔드 서버 배포
3. 프론트엔드 배포

## 📚 주요 기능

- ✅ 메뉴 관리 (활성/시즌오프 상태 변경)
- ✅ 주문하기 (옵션 선택 포함)
- ✅ 주문 현황 조회 (팀별/인원별)
- ✅ 추천 메뉴 (주문 패턴 기반)
- ✅ 메뉴 카테고리별 필터링

## 🛠️ 기술 스택

### 백엔드
- Node.js + Express
- PostgreSQL
- pg (PostgreSQL 클라이언트)

### 프론트엔드
- React 18
- Vite
- Axios

## 📝 라이선스

ISC
